@model IEnumerable<MilestoneProject.Models.Fighter>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.wins)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.losts)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.draws)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.weightCategory)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.wins)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.losts)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.draws)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.weightCategory.name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FighterId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FighterId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FighterId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
